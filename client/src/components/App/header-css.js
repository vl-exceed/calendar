const header = {
    height: '40px',
    width: '100%',
    display: 'flex',
    justifyContent: 'space-between',
    borderBottom: '1px solid rgb(83, 161, 235)'
}

const label = {
    fontSize: '20px',
    margin: 'auto 0 auto 6px'
}

const dropDownMenu = {

}

const userBlock = {
    height: '40px',
    display: 'flex'
}

const userName = {
    fontSize: '20px',
    margin: 'auto 6px auto 0'
}

const menuItems = {
    zIndex: '1',
    position: 'absolute'
}

const menuItem = {
    backgroundColor: 'blue'
}

module.exports = {
    header,
    label,
    dropDownMenu,
    userBlock,
    userName,
    menuItems,
    menuItem
} 